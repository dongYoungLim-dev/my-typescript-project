function sum(a: number, b: number): number {
	return a + b;
}

var result = sum(20, 20);


result.toLocaleString();

// vscode IntelliSense => vscode 자동완성 기을을 타입을 설정함으로 설정한 타입의 API 를 VScode IntelliSense가 추론 하여 보여 준다.

